export const BASE_URL = import.meta.env.VITE_API_BASE_URL;

// utils/apiPaths.js
export const API_PATHS = {
  DASHBOARD: {
    GET_MAKES: "/api/makes", // Placeholder only Will be changed later
    GET_MODELS: "/api/models", // Placeholder only Will be changed later
    GET_TYPES: "/api/types", // Placeholder only Will be changed later
    GET_PARTS: "/api/parts", // Placeholder only Will be changed later
  },
};
